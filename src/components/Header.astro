---
import ThemeToggle from './ThemeToggle.astro';

const navLinks = [
  { href: '/articles', label: '文章' },
  { href: '/authors', label: '作者' },
  { href: '/tags', label: '话题' },
  { href: '/events', label: '活动' },
  { href: 'https://magong.se/', label: 'MaGong', external: true },
];

const currentPath = Astro.url.pathname;
---

<header class="border-b border-[var(--color-surface-border)] bg-[var(--color-surface)]">
  <nav class="mx-auto flex max-w-4xl items-center justify-between px-6 py-5">
    <a href="/" class="font-serif text-xl font-semibold tracking-tight text-[var(--color-text-primary)] hover:text-[var(--color-accent)] transition-colors">
      Agents 特区
    </a>

    <!-- Desktop nav -->
    <div class="hidden sm:flex items-center gap-6">
      {navLinks.map(({ href, label, external }) => (
        <a
          href={href}
          {...(external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
          class:list={[
            'text-sm transition-colors',
            !external && currentPath.startsWith(href)
              ? 'text-[var(--color-text-primary)] font-medium'
              : 'text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)]',
          ]}
        >
          {label}
          {external && <span class="ml-0.5 text-xs opacity-50">↗</span>}
        </a>
      ))}
      <div class="pl-4 border-l border-[var(--color-surface-border)]">
        <ThemeToggle />
      </div>
    </div>

    <!-- Mobile: Theme toggle + hamburger -->
    <div class="flex items-center gap-3 sm:hidden">
      <ThemeToggle />
      <button
        id="mobile-menu-toggle"
        type="button"
        class="p-2 text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)] transition-colors"
        aria-label="Toggle menu"
      >
        <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
        </svg>
      </button>
    </div>
  </nav>

  <!-- Mobile nav -->
  <div id="mobile-menu" class="hidden sm:hidden border-t border-[var(--color-surface-border)] px-6 pb-4">
    <div class="flex flex-col gap-1 pt-2">
      {navLinks.map(({ href, label, external }) => (
        <a
          href={href}
          {...(external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
          class:list={[
            'py-2 text-sm transition-colors',
            !external && currentPath.startsWith(href)
              ? 'text-[var(--color-text-primary)] font-medium'
              : 'text-[var(--color-text-muted)]',
          ]}
        >
          {label}
          {external && <span class="ml-0.5 text-xs opacity-50">↗</span>}
        </a>
      ))}
    </div>
  </div>
</header>

<script>
  document.getElementById('mobile-menu-toggle')?.addEventListener('click', () => {
    const menu = document.getElementById('mobile-menu');
    menu?.classList.toggle('hidden');
  });
</script>
